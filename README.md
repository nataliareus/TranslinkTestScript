# TranslinkTestScript
test scripts need to be run in Visual studio 2022 with selenuim webdriver, selenium support packages. 
Load the solution and compile. 
select the test in the upper menu and run all test.
